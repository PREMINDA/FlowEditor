
export const DEFAULT_PROCESS_NAME = 'My Process';
export const DEFAULT_PROCESS_FILENAME = 'process.flowchartprocess.json';
export const DEFAULT_PROCESS_TYPE = 'processNode';

export const DEFAULT_FLOW_DATA = {
    nodes: [],
    edges: [],
    blackboard: {
        variables: []
    }
};

export const DEFAULT_NODE_STYLES = {
    width: 60,
    height: 60,
};
